# @vividcodeai/embedded-xlink-darwin-arm64

适用于 macOS Apple Silicon 的预编译二进制文件。这是 `@vividcodeai/embedded-xlink-mcp` 的平台依赖包。

**不要直接安装此包。** 请安装父包：

```bash
npm install @vividcodeai/embedded-xlink-mcp
```

---

[English Documentation](README.md)
